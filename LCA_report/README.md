# Sample LCA reports

PDF reports generated by this public modeling pipeline (Excel inventory → openLCA product system → LCIA).

These files are examples of Web / PDF report output. They are not input inventories (Excel files remain gitignored).
